// el tipo de dato antes del puntero quiere decir que el valor que haya en esa direccion se tiene q
// interpretar como ese mismo tipo de dato.
// en el caso del ejemplo, el valor que se encuentra en la direccion 0xF0 se tiene que
// interpretar como entero sin signo.